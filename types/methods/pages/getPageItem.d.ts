import { IPageItem } from '../../types';
/**
 * Gets the pageItem from the current context
 *
 * @returns {Promise<IPageItem>}
 */
export declare const getPageItem: () => (Promise<IPageItem> | undefined);
