import { IAppFieldValue } from "../../types";
/**
 * setFieldValue
 *  sends a message out to the iframe channel with a a name and value
 * @param param0
 * @returns
 */
export declare const setFieldValue: ({ name, value }: IAppFieldValue) => void;
